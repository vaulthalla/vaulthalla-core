#pragma once

namespace vh::database {

class ACLQueries {
  public:
    // Add your ACL-related query methods here
    // For example:
    // static void addPermissionToRole(unsigned int roleId, unsigned int permissionId);
    // static void removePermissionFromRole(unsigned int roleId, unsigned int permissionId);
    // static std::vector<unsigned int> getPermissionsForRole(unsigned int roleId);
    // static bool hasPermission(unsigned int userId, unsigned int permissionId);
  private:
    // Private helper methods if needed
  };

}
