name: 'Test Web'
description: 'Runs web frontend CI checks.'

runs:
  using: "composite"
  steps:
    - name: Test web
      shell: bash
      working-directory: ${{ github.workspace }}/web
      run: pnpm test
